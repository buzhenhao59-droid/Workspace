# ============================================================
# 买方AI客服系统 - 生产环境依赖
# ============================================================
# 此文件仅用于参考，实际使用 .venv 的依赖
# 买方系统和卖方系统共用同一个 .venv 虚拟环境

fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic>=2.10,<3
pydantic-settings>=2.4,<3
python-dotenv==1.0.0

# 数据库（共用卖方系统的 gold_customer.db）
# Neo4j（图数据库）
neo4j==5.16.0

# 实时通信
websockets>=12.0
python-multipart==0.0.6

# 外部 API
requests==2.31.0
httpx>=0.25.0

# 可观测性
prometheus-client>=0.19.0
psutil>=5.9.0
# Sentry 错误追踪 (https://sentry.io)
sentry-sdk>=0.20.0
